import type { Meta, StoryObj } from '@storybook/react';
import {{pascalNombre}} from './{{pascalNombre}}';

const meta: Meta<typeof Text> = {
  title: '{{storyGroup}}/{{pascalNombre}}',
  component: {{pascalNombre}},
  parameters: {
    docs: {
      autodocs: true
    }
  },
  tags: ['autodocs']
};
export default meta;

type Story = StoryObj<typeof {{pascalNombre}}>;

export const Default: Story = {
  args: {}
};
