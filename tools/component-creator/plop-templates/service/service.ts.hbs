import { api } from '@config/axios';
import type { {{pascalCase http}}{{pascalNombre}}Props, {{pascalNombre}}ResponseProps } from '../models/api/{{camelNombre}}ServiceTypes';

export const {{kebabCase http}}{{pascalNombre}} = async (args: {{pascalCase http}}{{pascalNombre}}Props): Promise<{{pascalNombre}}ResponseProps[]> => {
  const environment = import.meta.env.VITE_ENVIRONMENT;

  if (environment === 'DEV') {
    const queryParams = new URLSearchParams(args as Record<string, string>).toString();
    const response = await fetch(`/mock/{{camelNombre}}?${queryParams}`);
    const data = await response.json();
    return data;
  } else {
    const response = await api.{{kebabCase http}}('/{{camelNombre}}', { params: args });
    const data = response.data;
    return data;
  }
};